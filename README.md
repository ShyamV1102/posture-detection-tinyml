# posture-detection-tinyml
Posture Detection TinyML – a small project that spots good vs bad posture using motion data from an Arduino Nano 33 BLE. The repo includes the sensor recordings, a notebook that trains a simple model in Google Colab, the exported model files, and an Arduino sketch that runs the posture check directly on the board.
